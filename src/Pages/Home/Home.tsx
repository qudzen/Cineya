import {Link} from 'react-router-dom'import SliderFilm from "./SliderFilm.tsx";import fetchPopularFilm from "../../api.tsx";import SetFilm from "../Components/SetFilm.tsx";import FilmGrid from "../Components/FilmGrid.tsx";import SectionHeader from "../Components/SectionHeader.tsx";import type {Result} from "../../type.tsx";import {useFetch} from "../Hooks/useFetch.tsx";const shuffle = (array: Result[]) => {    return [...array].sort(() => Math.random() - 0.5)}export default function Home() {    const {data, error, loading} = useFetch(fetchPopularFilm)    if (loading) return <span className="loading loading-spinner loading-xl"></span>    if (error) return <div>Не удалось загрузить фильмы.</div>    const shuffled = shuffle(data?.results || [])    const sliderFilms = shuffled.slice(0, 5)    const otherFilms = shuffled.slice(5)    const featuredFilms = [...otherFilms]        .sort((a, b) => b.vote_average - a.vote_average)        .slice(0, 3)    const featuredIds = new Set(featuredFilms.map(f => f.id))    const gridFilms = otherFilms.filter(f => !featuredIds.has(f.id))    return (        <div>            {sliderFilms.length > 0 && (                <SliderFilm sliderFilms={sliderFilms}/>            )}            {otherFilms.length > 0 && (                <section className="relative mt-8 border-t border-white/10 bg-gradient-to-b from-white/[0.04] to-transparent pb-24 pt-10 md:pb-12">                    <div className="pointer-events-none absolute inset-x-0 top-0 h-px bg-gradient-to-r from-transparent via-yellow-400/40 to-transparent"/>                    <SectionHeader title="Популярные фильмы" subtitle="Топ по рейтингу"/>                    <div className="mx-auto mt-8 grid max-w-[1600px] grid-cols-1 gap-3 px-4 md:grid-cols-3 lg:px-12">                        {featuredFilms.map((film) => (                            <Link key={film.id} to={`/movie/${film.id}`} className="h-full">                                <SetFilm film={film} variant="featured"/>                            </Link>                        ))}                    </div>                    {gridFilms.length > 0 && (                        <div className="mt-10">                            <FilmGrid films={gridFilms}/>                        </div>                    )}                </section>            )}        </div>    )}